---
"@kilocode/kilo-jetbrains": patch
---

Keep JetBrains stability records in the same IDE scope file across upgrades and forced restarts without blocking the UI, and clear pending records when collection is disabled.
